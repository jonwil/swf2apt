namespace eaf2apt
{
    class AptExport
    {
        public ushort ID;
        public string Name;
        public AptExport(ushort id, string name)
        {
            ID = id;
            Name = name;
        }
    }
}
